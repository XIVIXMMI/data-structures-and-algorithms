fn main() {
    println!("Hello World"); 
}

// " ! " -> is call a Rust macro if not have " ! " -> this mean calling function 
// Macro mean "code that generates code", just use ! and the Macro will expand to a lot line of code before

// " ; " -> indicate that this expression is over!



