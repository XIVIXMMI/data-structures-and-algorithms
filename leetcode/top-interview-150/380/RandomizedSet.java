/**
 *
 * INSERT DELETE GETRANDOM O(1)
 *
 * Implement the RandomizedSet class:
 * - `RandomizeSet()` initializes the RandomizeSet object
 * - `bool insert(int val)` inserts an item `val` into the set if not present
 * Returns `true` if the item was not present, `false` otherwise
 * - `bool remove(int val)` Removes an item `val` from the set if present
 * Return `true` if the item was present, `false` otherwise
 * - `int getRandom()` Returns a random element from the current set of elements
 * (it's guaranteed that at least one element exist when this method is called)
 * Each element must have the *same probability* of being Returns
 *
 * You must implement the functions of the class such that each function works
 * in *average* `O(1)` time complexity
 *
 */

class RandomizedSet {

  public RandomizedSet() {

  }

  public boolean insert(int val) {

  }

  public boolean remove(int val) {

  }

  public int getRandom() {

  }
}

/**
 * Your RandomizedSet object will be instantiated and called as such:
 * RandomizedSet obj = new RandomizedSet();
 * boolean param_1 = obj.insert(val);
 * boolean param_2 = obj.remove(val);
 * int param_3 = obj.getRandom();
 */
